---
interface Props {
  id?: string;
  /** "light" = weiß, "canvas" = sehr helles Grau, "dark" = Anthrazit/Navy */
  tone?: 'light' | 'canvas' | 'dark';
  class?: string;
}

const { id, tone = 'light', class: className = '' } = Astro.props;

const tones = {
  // "light" ist die erhobene weisse Flaeche, "canvas" die Grundflaeche der
  // Seite. Die feinen Trennlinien machen den Wechsel als Kante sichtbar.
  light: 'bg-surface text-ink border-y border-line',
  canvas: 'bg-canvas text-ink verlauf-flaeche',
  dark: 'on-dark bg-ink-deep text-white',
} as const;
---

<section id={id} class:list={['py-20 md:py-28', tones[tone], className]}>
  <div class="container-page">
    <slot />
  </div>
</section>
