name: Youman - Build Installer

on:
  push:
    branches:
      - main
      - "claude/palette-optimizer-build-**"
    paths:
      - "paletten-optimierer/mini/**"
      - "paletten-optimierer/assets/**"
      - ".github/workflows/paletten-mini.yml"
  workflow_dispatch:
    inputs:
      version:
        description: "Version (z.B. 1.0.0)"
        required: true
        default: "1.0.0"

defaults:
  run:
    working-directory: paletten-optimierer/mini

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Version bestimmen
        id: vars
        shell: pwsh
        run: |
          $version = "${{ inputs.version }}"
          if ([string]::IsNullOrWhiteSpace($version)) { $version = "1.0.0" }
          Write-Host "Version: $version"
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: paletten-optimierer/mini/requirements.txt

      - name: Dependencies installieren
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build-Identitaet einbrennen
        shell: pwsh
        env:
          APP_VERSION: ${{ steps.vars.outputs.version }}
        run: |
          $sha = "${{ github.sha }}".Substring(0, 7)
          $when = (Get-Date).ToUniversalTime().ToString("yyyy-MM-dd HH:mm UTC")
          $ver = $env:APP_VERSION
          $content = @"
          # Auto-generated by GitHub Actions
          from __future__ import annotations

          BUILD_SHA: str = "$sha"
          BUILD_DATE: str = "$when"
          BUILD_VERSION: str = "$ver"
          "@
          Set-Content -Path "_build_info.py" -Value $content -Encoding UTF8
          Write-Host "Build-Stempel: $sha / $when / v$ver"

      - name: Pflicht-Test Kern Modell A (v4, Cluster + Deckel-Obergrenze)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_kern_modell_a.py

      - name: Pflicht-Test Struktur (Code-erzeugte Fixtures)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_kern_struktur.py

      - name: Optionaler Test Echtdatei (Skip wenn Paletten_DM.xlsx fehlt)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_kern_echt.py

      - name: Pflicht-Test Import-Produkt (Domain-Fix P-Werte -> Produkt)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_import_produkt.py

      - name: Pflicht-Test Toggle-Wiring (UI -> Kern v4 1:1)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_toggle_wiring.py

      - name: Pflicht-Test Typ-Anzeige (4 Typen + Farbcodes)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_typ_anzeige.py

      - name: Pflicht-Test Import-Verlauf
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_verlauf.py

      - name: Pflicht-Test Palettenkatalog (Tie-Breaker + CRUD)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_katalog.py

      - name: Pflicht-Test Toleranz-Modus (mm / %)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_toleranz_modus.py

      - name: Pflicht-Test Sonderpaletten-Option + Score-Breakdown
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_sonder_score.py

      - name: Pflicht-Test Bestandsverwaltung + Dashboard-Workflow
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_bestand_workflow.py

      - name: Pflicht-Test Bestellungs-Historie (Spec §7)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_bestellungen.py

      - name: Pflicht-Test Doppelbestellungs-Vermeidung (Spec §8)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_doppelschutz.py

      - name: Pflicht-Test Wirtschaftlichkeit + LKW-BinPacking (Spec §11)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_wirtschaftlichkeit.py

      - name: Pflicht-Test Verbrauch + Beschaffung (Spec §9 + §10)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_verbrauch_und_beschaffung.py

      - name: Pflicht-Test Logistikkosten 3-Modi + Lademeter (Spec §11.1+§11.2)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_wirt_logistikmodi.py

      - name: Pflicht-Test Modus-2 Kandidaten + Anti-Aggregation (Spec §11.9)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_wirt_modus2.py

      - name: Pflicht-Test Bestellungen-Tab (BST-Nr, Teilmengen, Idempotenz)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_bestellungen_tab.py

      - name: Pflicht-Test Kostenanalyse (Szenarien + Snapshot + Verlauf-Loesch)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_kostenanalyse.py

      - name: Pflicht-Test Berichte (PDF + Excel + Archiv)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_berichte.py

      - name: Pflicht-Test 9-Feature-Erweiterungen (Auto-Std, Dup, EK-Vgl, etc.)
        shell: pwsh
        env: { PYTHONIOENCODING: utf-8 }
        run: python tests/test_erweiterungen.py

      - name: Selbsttest-Status anzeigen
        shell: pwsh
        run: |
          if (Test-Path "selbsttest_status.json") {
            Write-Host "=== Selbsttest-Status (wird in EXE eingebrannt) ==="
            Get-Content "selbsttest_status.json"
          } else {
            Write-Host "WARN: kein selbsttest_status.json gefunden"
          }

      - name: Icon aus Youman-Logo erzeugen (icon.ico)
        shell: pwsh
        working-directory: paletten-optimierer/mini/build
        run: python ../tools/make_icon.py ../../assets/youman_logo.png icon.ico

      - name: PyInstaller-Build
        shell: pwsh
        working-directory: paletten-optimierer/mini/build
        run: pyinstaller paletten-mini.spec --noconfirm --clean

      - name: Inno Setup installieren
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Setup.exe bauen
        shell: pwsh
        working-directory: paletten-optimierer/mini/build
        env:
          APP_VERSION: ${{ steps.vars.outputs.version }}
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

      - name: Artifact hochladen
        uses: actions/upload-artifact@v4
        with:
          name: Youman-Setup-${{ steps.vars.outputs.version }}
          path: paletten-optimierer/mini/build/output/*.exe
          if-no-files-found: error
          retention-days: 30
