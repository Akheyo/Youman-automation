/**
 * Ambient declarations for non-code imports.
 *
 * Cesium is loaded via <Script> in app/layout.tsx, so it isn't imported
 * from the npm package directly — no module declaration needed.
 */

declare module '*.css';
declare module '*.scss';
