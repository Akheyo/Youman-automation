"""Build-Identität (Mini-App).

Wird vom CI-Workflow vor PyInstaller mit echten Werten überschrieben.
"""
from __future__ import annotations

BUILD_SHA: str = "dev"
BUILD_DATE: str = "lokal"
BUILD_VERSION: str = "dev"
